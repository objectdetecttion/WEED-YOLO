# Ultralytics 🚀 AGPL-3.0 License - https://ultralytics.com/license

# MIE-YOLO backbone (MS-EIS / Add-CGLU) + official Pose head
# For custom pose/keypoint datasets (YOLO pose labels: cls cx cy w h kx ky vis ...)

# Parameters
nc: 3 # number of classes (set to your dataset)
kpt_shape: [1, 3] # number of keypoints, number of dims (3 = x, y, visible)
scales: # model compound scaling constants, i.e. 'model=MIE-pose.yaml' will call MIE-pose.yaml with scale 'n'
  # [depth, width, max_channels]
  n: [0.50, 0.25, 1024]
  s: [0.50, 0.50, 1024]
  m: [0.50, 1.00, 512]
  l: [1.00, 1.00, 512]
  x: [1.00, 1.50, 512]

# Backbone (identical to MIE-YOLO.yaml, P1-P5 with MS-EIS blocks)
backbone:
  # [from, repeats, module, args]
  - [-1, 1, Conv,  [64, 3, 2]] # 0-P1/2
  - [-1, 1, Conv,  [128, 3, 2, 1, 2]] # 1-P2/4
  - [-1, 2, C3k2_MutilScaleEdgeInformationSelect, [256, False, 0.25]]
  - [-1, 1, Conv,  [256, 3, 2, 1, 4]] # 3-P3/8
  - [-1, 2, C3k2_MutilScaleEdgeInformationSelect, [512, False, 0.25]]
  - [-1, 1, Conv,  [512, 3, 2]] # 5-P4/16
  - [-1, 2, C3k2_MutilScaleEdgeInformationSelect, [512, True]]
  - [-1, 1, Conv,  [1024, 3, 2]] # 7-P5/32
  - [-1, 2, C3k2_MutilScaleEdgeInformationSelect, [1024, True]] # 8

# Neck (identical to MIE-YOLO.yaml, P3/P4/P5 with Add-CGLU blocks)
head:
  - [-1, 1, nn.Upsample, [None, 2, "nearest"]]
  - [[-1, 6], 1, Concat, [1]] # cat backbone P4
  - [-1, 2, C3k2_AdditiveBlock_CGLU, [512, False]] # 11

  - [-1, 1, nn.Upsample, [None, 2, "nearest"]]
  - [[-1, 4], 1, Concat, [1]] # cat backbone P3
  - [-1, 2, C3k2_AdditiveBlock_CGLU, [256, False]] # 14 (P3/8-small)

  - [-1, 1, Conv, [256, 3, 2]]
  - [[-1, 11], 1, Concat, [1]] # cat head P4
  - [-1, 2, C3k2_AdditiveBlock_CGLU, [512, False]] # 17 (P4/16-medium)

  - [-1, 1, Conv, [512, 3, 2]]
  - [[-1, 8], 1, Concat, [1]] # cat head P5
  - [-1, 2, C3k2_AdditiveBlock_CGLU, [1024, True]] # 20 (P5/32-large)

  - [[14, 17, 20], 1, Pose, [nc, kpt_shape]] # Pose(P3, P4, P5)
